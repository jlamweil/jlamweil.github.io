# Domain Documentation

**Layout**: Single-context
**CONTEXT.md**: Not present (personal website — no complex domain model)
**ADRs**: None

## Consumer Rules

1. This is a simple static personal website (GitHub Pages).
2. There is no multi-module or monorepo structure — a single `index.html` with embedded CSS/JS.
3. No architecture decision records (ADRs) exist because the project has no architectural decisions to document yet.
4. Future domain knowledge (e.g., notes content taxonomy, publishing workflow) should be recorded inline or in the PRD.
